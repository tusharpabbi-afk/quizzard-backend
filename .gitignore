# Secrets / env
.env
.env.*

# Dependencies (Render/npm install rebuilds these)
node_modules/

# OS noise
.DS_Store
**/.DS_Store

# Firebase deploy cache
quizzard_app/.firebase/

# Flutter / Dart build artifacts
quizzard_app/build/
quizzard_app/.dart_tool/
quizzard_app/.flutter-plugins
quizzard_app/.flutter-plugins-dependencies
